{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Iniciar o Chrome em relação a 127.0.0.1",
            "url": "http://127.0.0.1:5500", 
            "webRoot": "${workspaceFolder}"
        }
    ]
}
